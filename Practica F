#include <iostream>
#include <sstream>
#include <string>
#include <iomanip>
#include <algorithm>
using namespace std;

int main()
{
	//1
	for (int i = 9;
		i >= 0; i--)
	{
		cout << i << ",";
	}
	cout << endl;

	//2
	for (int i = 0;
		i <= 99; i++)
	{
		cout << i << ",";
	}
	cout << endl;

	//3
	for (int i = 0;
		i <= 100; i++)

	{
		if (i % 2 == 0) {
			cout << i << ",";
		}
	}
	cout << endl;

	//4
	int min, max;

	cout << "ingresa valor min: ";
	cin >> min;
	cout << "Ingrese valor max: ";
	cin >> max;

	for (int i = min;
		i <= max; i++)
	{
		cout << i << ",";
	}
	cout << endl;

	//5
	int MIN, MAX;

	cout << "ingresa valor min: ";
	cin >> MIN;
	cout << "Ingrese valor max: ";
	cin >> MAX;

	for (int i = MAX;
		i >= MIN; i--)
	{
		cout << i << ",";
	}
	cout << endl;

	return 0;
}
